// Shared column widths so the per-qty pricing cells in material/operation cards
// line up under the Qty inputs in the sticky toolbar (acceptance #4).

/** Width (px) of each Qty input and each per-qty pricing column. */
export const QTY_COL_W = 88;
/** Width (px) of the far-right gutter: toolbar "+" button / card icon stack. */
export const ROW_ICON_W = 36;
